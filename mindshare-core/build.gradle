version = '1.0.0'

dependencies {

}

tasks {
    bootJar {
        enabled = false // Spring Boot JAR 비활성화
    }

    jar {
        enabled = true
    }
}

tasks.named('test') {
    useJUnitPlatform()
}
